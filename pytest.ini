[pytest]
# Caminho para pg_ctl do PostgreSQL 16 instalado via Homebrew
postgresql_exec = /opt/homebrew/opt/postgresql@16/bin/pg_ctl

# Marcadores customizados
markers =
    integration: testes de integração que requerem PostgreSQL real (pytest-postgresql)
